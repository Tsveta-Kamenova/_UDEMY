import tkinter as tk

window = tk.Tk()

w = tk.Label(window, text="Love")
w.place(x=0, y=0)
w = tk.Label(window, text="grows")
w.place(x=50, y=10)
w = tk.Label(window, text="everywhere")
w.place(x=100, y=20)




window.mainloop()
